version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://backend:5241
    depends_on:
      - backend
    networks:
      - appnet

  backend:
    build:
      context: ./backend
    ports:
      - "5241:5241"
    networks:
      - appnet

networks:
  appnet:
    driver: bridge 